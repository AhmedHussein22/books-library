class SaveLocalData {
  //* This method is used to save user data in local storage
  static Future<void> saveUseData() async {
    await Future.wait([]);
  }

//* This method is used to clear user data from local storage
  static Future<void> clearUserData() async {
    await Future.wait([]);
  }
}
